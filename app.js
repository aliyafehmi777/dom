let doc;
doc = document;
doc = document.all;
doc = document.all[1];
doc = document.all.length;

doc = document.URL;
doc = document.doctype;
doc = document.domain;
doc = document.contentType;

doc = document.links;
doc = document.scripts;

console.log(doc)